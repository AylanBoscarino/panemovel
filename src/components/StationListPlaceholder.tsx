import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class StationListPlaceholder extends Component {
  render() {
    return (
      <View>
        <Text> Carregando </Text>
      </View>
    )
  }
}
